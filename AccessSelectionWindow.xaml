<Window x:Class="Accounting.AccessSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Pilih Konteks Akses"
        Width="560"
        Height="370"
        MinWidth="520"
        MinHeight="340"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="18"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Pilih Konteks Kerja"
                       FontFamily="Bahnschrift"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource Brush.TextPrimary}"/>
            <TextBlock Margin="0,8,0,0"
                       FontSize="14"
                       Foreground="{DynamicResource Brush.TextSecondary}"
                       Text="Pilih perusahaan dan lokasi untuk melanjutkan."/>
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" VerticalAlignment="Center" Style="{StaticResource FieldLabelTextStyle}" Text="Perusahaan"/>
            <ComboBox Grid.Column="1"
                      Height="40"
                      Padding="8,4"
                      BorderThickness="1.2"
                      BorderBrush="{DynamicResource Brush.Border}"
                      Background="{DynamicResource Brush.Surface}"
                      Foreground="{DynamicResource Brush.TextPrimary}"
                      ItemsSource="{Binding CompanyOptions}"
                      SelectedItem="{Binding SelectedCompany, Mode=TwoWay}"
                      IsEnabled="{Binding IsCompanyMulti}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Foreground="{DynamicResource Brush.TextPrimary}">
                            <Run Text="{Binding Code}"/>
                            <Run Text=" - "/>
                            <Run Text="{Binding Name}"/>
                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" VerticalAlignment="Center" Style="{StaticResource FieldLabelTextStyle}" Text="Lokasi"/>
            <ComboBox Grid.Column="1"
                      Height="40"
                      Padding="8,4"
                      BorderThickness="1.2"
                      BorderBrush="{DynamicResource Brush.Border}"
                      Background="{DynamicResource Brush.Surface}"
                      Foreground="{DynamicResource Brush.TextPrimary}"
                      ItemsSource="{Binding LocationOptions}"
                      SelectedItem="{Binding SelectedLocation, Mode=TwoWay}"
                      IsEnabled="{Binding IsLocationMulti}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Foreground="{DynamicResource Brush.TextPrimary}">
                            <Run Text="{Binding Code}"/>
                            <Run Text=" - "/>
                            <Run Text="{Binding Name}"/>
                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>

        <TextBlock Grid.Row="4"
                   Margin="0,14,0,0"
                   FontSize="12"
                   Foreground="{DynamicResource Brush.Error}"
                   TextWrapping="Wrap"
                   Text="{Binding ErrorMessage}"/>

        <Grid Grid.Row="5" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1"
                    Height="42"
                    MinWidth="100"
                    Click="CancelButton_OnClick"
                    Style="{StaticResource SecondaryPillButtonStyle}"
                    Content="Batal"/>
            <Button Grid.Column="3"
                    Height="42"
                    MinWidth="120"
                    IsDefault="True"
                    Click="ContinueButton_OnClick"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Content="Lanjutkan"/>
        </Grid>
    </Grid>
</Window>

