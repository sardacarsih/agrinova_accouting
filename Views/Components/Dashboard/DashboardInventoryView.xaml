<UserControl x:Class="Accounting.Views.Components.Dashboard.DashboardInventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">
    <Border Style="{StaticResource DashboardSectionCardStyle}">
        <StackPanel>
            <TextBlock Style="{StaticResource DashboardSectionTitleTextStyle}" Text="Inventory Summary" />
            <TextBlock Style="{StaticResource DashboardSectionSubtitleTextStyle}" Text="Nilai persediaan, item bergerak, dan low stock alert." />

            <WrapPanel Margin="0,14,0,0">
                <Border Style="{StaticResource SummaryCardStyle}" Width="190" Margin="0,0,10,10">
                    <StackPanel>
                        <TextBlock Style="{StaticResource MetaCaptionTextStyle}" Text="Total Inventory Value" />
                        <TextBlock Style="{StaticResource DashboardMiniValueTextStyle}" Text="{Binding Inventory.TotalValue, StringFormat=N0}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource SummaryCardStyle}" Width="160" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Style="{StaticResource MetaCaptionTextStyle}" Text="Low Stock Count" />
                        <TextBlock Style="{StaticResource DashboardMiniValueTextStyle}" Foreground="{DynamicResource Brush.Warning}" Text="{Binding Inventory.LowStockCount}" />
                    </StackPanel>
                </Border>
            </WrapPanel>

            <TextBlock Margin="0,8,0,8"
                       Style="{StaticResource FieldLabelTextStyle}"
                       Text="Top moving items" />
            <ItemsControl ItemsSource="{Binding Inventory.TopMovingItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,8" Padding="10" Background="{DynamicResource Brush.SurfaceAlt}" CornerRadius="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold">
                                        <Run Text="{Binding ItemCode}" />
                                        <Run Text=" - " />
                                        <Run Text="{Binding ItemName}" />
                                    </TextBlock>
                                    <TextBlock Style="{StaticResource MetaCaptionTextStyle}" Text="{Binding Uom, StringFormat=UoM: {0}}" />
                                </StackPanel>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{Binding Qty, StringFormat=N0}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border Margin="0,0,0,12"
                    Padding="14"
                    CornerRadius="12"
                    Background="{DynamicResource Brush.SurfaceAlt}"
                    BorderBrush="{DynamicResource Brush.Border}"
                    BorderThickness="1">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Inventory.TopMovingItems.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Foreground="{DynamicResource Brush.TextSecondary}"
                           Text="Belum ada data pergerakan item untuk periode ini." />
            </Border>

            <TextBlock Margin="0,12,0,8"
                       Style="{StaticResource FieldLabelTextStyle}"
                       Text="Low stock items" />
            <ItemsControl ItemsSource="{Binding Inventory.LowStockItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,8" Padding="10" Background="{DynamicResource Brush.ErrorSubtle}" CornerRadius="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold">
                                        <Run Text="{Binding ItemCode}" />
                                        <Run Text=" - " />
                                        <Run Text="{Binding ItemName}" />
                                    </TextBlock>
                                    <TextBlock Style="{StaticResource MetaCaptionTextStyle}" Text="{Binding LocationName}" />
                                </StackPanel>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{Binding Qty, StringFormat=N2}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border Margin="0,0,0,0"
                    Padding="14"
                    CornerRadius="12"
                    Background="{DynamicResource Brush.SurfaceAlt}"
                    BorderBrush="{DynamicResource Brush.Border}"
                    BorderThickness="1">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Inventory.LowStockItems.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Foreground="{DynamicResource Brush.TextSecondary}"
                           Text="Tidak ada item low stock pada filter aktif." />
            </Border>
        </StackPanel>
    </Border>
</UserControl>
